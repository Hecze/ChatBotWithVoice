import Link from "next/link";

const Navbar = () => {

    const routes = [
        { href: "/register", label: "Crea tu cuenta" },
        { href: "/login", label: "Ingresa" },
        { href: "/MisCompras", label: "Mis Compras" },
    ];

    return (
        <>
        </>

    )
}

export default Navbar;